let URL = "https://project-kruma.glitch.me"
let githubPage = "/kruma/frontend"
if (window.location.host == "localhost:3000") {
    URL = "http://localhost:3000"
    githubPage = "http://localhost:3000/"
}